/* These are supposed to be provided by electron-forge.
 * That doesn't seem to WORK, but we'll check them anyway.
 */
declare const MAIN_WINDOW_WEBPACK_ENTRY: string;
declare const MAIN_WINDOW_PRELOAD_WEBPACK_ENTRY: string;
